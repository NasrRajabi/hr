{% import "forms_helpers.twig" as forms %}
{% extends "master.twig" %}
{% block main %}
    <div class="row justify-content-md-center">
        <div class="row ">
            <div class="card">
                <div class="row justify-content-between  p-2 ">
                    <div class="col-md-6">
                        <h4 class="text-end me-3 mt-3">
                          اضافة قائمة جديدة (الاقسام الرئيسية)
                        </h4>
                    </div>
                    <div class="col-md-6">
                        <nav class="nav justify-content-end p-2">
                            <a type="button" class="btn btn-info ms-2" href="/menus">
                                <i class="bi bi-eye"></i>
                                عرض الكل
                            </a>
                        </nav>
                    </div>
                </div>
                <div class="card-body">
                    <form action="/menus/add" method="post">
                        {{ csrf.fields | raw }}
                        <div class="col-6 mx-auto">
                        <div class="mb-3">
                            <label for="menu_name" class="form-label">اسم القائمة</label>
                            <input type="text"  name="menu_name" id="menu_name" value="{{ old.menu_name }}" class="form-control {{ errors.menu_name ? 'is-invalid' : '' }}">
                            <div class="invalid-feedback">
                                {{ errors.menu_name | first }}
                            </div>
                        </div>
                        </div>
                        <div class=" col-6 mx-auto">
                        <div class="mb-3">
                            <label for="description" class="form-label">الوصف</label>
                            <input type="text"  name="description" id="description" value="{{ old.description }}" class="form-control {{ errors.description ? 'is-invalid' : '' }}">
                            <div class="invalid-feedback">
                                {{ errors.description | first }}
                            </div>
                        </div>
                        </div>

                        <div class="d-grid gap-2 col-2 mx-auto m-5">
                            <button class="btn btn-primary" type="submit">حفظ</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}